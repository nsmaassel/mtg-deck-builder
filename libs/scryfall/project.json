{
  "name": "scryfall",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/scryfall/src",
  "projectType": "library",
  "tags": ["type:lib", "scope:scryfall"],
  "targets": {
    "test": {
      "executor": "@nx/vitest:test",
      "options": { "config": "libs/scryfall/vite.config.ts" }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": { "command": "tsc --noEmit -p libs/scryfall/tsconfig.json" }
    }
  }
}

